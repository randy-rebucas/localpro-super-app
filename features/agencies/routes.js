// Modular agencies feature routes
const express = require('express');
const router = express.Router();
const controller = require('./controller');
// TODO: Add agencies routes here
module.exports = router;
