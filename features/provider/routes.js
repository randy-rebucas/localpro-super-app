// Modular provider feature routes
const express = require('express');
const router = express.Router();
const controller = require('./controller');
// TODO: Add provider routes here
module.exports = router;
